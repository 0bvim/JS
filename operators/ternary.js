const	personAge = 17;
let	canVote = personAge >= 16 ? 'Can Vote' : "Can't vote";

console.log(canVote);

/*
 * with if/else
 *
 * if (personAge >= 16)
 * {
 * 	canVote = 'Can Vote'
 * }
 * else
 * {
 * 	canVote = "Can't Vote"
 * }
 */
